{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildRunner",
            "type": "shell",
            "command": "flutter",
            "args": [
                "pub",
                "run",
                "build_runner",
                "build",
                "--delete-conflicting-outputs"
            ]
        }
    ]
}
